---
"@codegraphy-dev/core": patch
"@codegraphy-dev/extension": patch
---

Keep package plugin rows stable across toggles, preserve plugin display names while disabled, remove obsolete plugin reordering controls, and keep plugin-owned data saves from dropping graph settings.
