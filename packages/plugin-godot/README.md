# CodeGraphy GDScript Plugin

Adds Godot GDScript dependency analysis to [CodeGraphy](https://marketplace.visualstudio.com/items?itemName=joesobo.codegraphy).

Requires the core CodeGraphy extension: `joesobo.codegraphy`.

Install this plugin alongside the core CodeGraphy extension to detect:

- `preload()`
- `load()`
- `extends`
- `class_name` references
