---
"@codegraphy-dev/extension": patch
---

Keep enabled symbol-kind nodes visible when graph visibility settings refresh after startup, and avoid rendering the initial graph until startup theme, settings, and plugin state are ready.
