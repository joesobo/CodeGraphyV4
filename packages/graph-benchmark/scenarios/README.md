# Obsidian reference scenarios

These versioned scenario files preserve the fixture, duration, and action sequence used to capture the committed artifacts under `../references/obsidian/`.

They are evidence provenance, not an active runner input. The current benchmark CLI does not parse or replay this schema. If replay support is added later, it should consume these files through a tested, explicit CLI workflow.
