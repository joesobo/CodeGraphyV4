// Generated by tests/acceptance/cucumber/generator.ts.
// Editor-only Cucumber step declarations for alexkrechik.cucumberautocomplete.
// The executable acceptance registry lives in ../steps.ts.
// Run pnpm --filter @codegraphy-dev/extension run generate:cucumber-steps after changing step text.

export {};

declare const Step: (expression: string | RegExp) => void;

Step("I open the examples/example-typescript workspace in VS Code");
Step("I open the CodeGraphy extension graph view");
Step("I see graph nodes");
Step("I see file nodes");
Step("I do not see edges");
Step("the workspace has no Graph Cache");
Step("the workspace still has no Graph Cache");
Step("I see folder nodes and Nests containment edges before indexing");
Step("I index the workspace");
Step("I have indexed the workspace");
Step("I see indexing progress");
Step("I see indexing progress disappear");
Step("the graph nodes have not changed");
Step("I see edges");
Step("I click the Graph Scope button");
Step("I see two buttons for switching views between node type and edge type toggles");
Step("I select edge types");
Step("I see a list of edge types with toggles");
Step("I toggle the Type imports edge on");
Step("I close the Graph Scope");
Step("src/main.ts points to src/App.vue");
Step("src/App.vue points to src/components/CounterPanel.vue");
Step("src/App.vue points to src/components/UserCard.vue");
Step("src/App.vue points to src/data/users.ts");
Step("src/App.vue points to src/composables/useCounter.ts");
Step("src/data/users.ts points to src/types.ts");
Step("src/components/UserCard.vue points to src/types.ts");
Step("src/components/CounterPanel.vue points to src/components/StatusBadge.vue");
Step("src/components/CounterPanel.vue points to src/composables/useCounter.ts");
Step("src/components/CounterPanel.vue points to src/types.ts");
Step("README.md is an orphan node");
Step(".gitignore is an orphan node");
Step("package.json is an orphan node");
Step("tsconfig.json is an orphan node");
Step("vite.config.ts is an orphan node");
Step("I see the src/index.ts node");
Step("the src/index.ts node is a blue circle");
Step("the src/index.ts node has a white TS symbol in the center of the node");
Step("the src/index.ts node has the file name \"index.ts\" as a label below the node");
Step("the src/index.ts node has an edge that points to the src/types.ts node");
Step("the src/index.ts node has an edge that points to the src/palette.ts node");
Step("the src/index.ts node has an edge that points to the src/utils.ts node");
Step("I hover the src/index.ts node");
Step("I see information for the src/index.ts node");
Step("the information says \"src/index.ts\" at the top");
Step("I click the src/index.ts node");
Step("the src/index.ts node is visibly outlined");
Step("src/index.ts opens in VS Code");
Step("I click and drag the src/index.ts node");
Step("the src/index.ts node moves");
Step("I stop dragging the src/index.ts node");
Step("the src/index.ts node stays where I drop it");
Step(/^I open the examples\/(.+) workspace in VS Code$/);
Step(/^I have indexed the workspace$/);
Step(/^I have not yet indexed the workspace$/);
Step(/^the graph nodes match the expected files in the examples\/(.+) workspace$/);
Step(/^I can see there are (\d+) nodes and (\d+) connections(?: displayed)?$/);
Step(/^I can see there are (\d+) nodes and (\d+) connection$/);
Step(/^the top right of the graph says "(\d+) nodes" and "(\d+) connections"$/);
Step(/^the top right of the graph says "(\d+) connections?"$/);
Step(/^(?!I see )(.+) points to (.+)$/);
Step(/^I see (.+) points to (.+)$/);
Step(/^(.+) has (\d+) edges pointing to (.+)$/);
Step(/^(.+) is an orphan node$/);
Step(/^(.+) is no longer an orphan node$/);
Step(/^I click the plugins button$/);
Step(/^I see a list of plugins with toggles$/);
Step(/^I toggle the (.+) plugin on$/);
Step(/^I right click the edge going from (.+) node to (.+) node to open its Graph Context Menu$/);
Step(/^I right click the (.+) node to open its Graph Context Menu$/);
Step(/^I right click the graph background to open its Graph Context Menu$/);
Step(/^I right click one of the folder nodes to open its Graph Context Menu$/);
Step(/^I right click one of the selected nodes to open its Graph Context Menu$/);
Step(/^I see the "(.+)" entry$/);
Step(/^I see the "Open x Files" entry, where x is the number of selected nodes$/);
Step(/^I see the "Delete x Files" entry, where x is the number of selected nodes$/);
Step(/^I select the "(.+)" entry$/);
Step(/^I click the "(.+)" entry$/);
Step(/^the (.+) file opens in VS Code as a preview editor tab$/);
Step(/^the (.+) file opens in VS Code as a pinned editor tab$/);
Step(/^I click the (.+) node to select it$/);
Step(/^I double click the (.+) node$/);
Step(/^I click the graph background to unselect the (.+) node$/);
Step(/^the (.+) node is visibly outlined in white$/);
Step(/^the (.+) node is visibly outlined in orange$/);
Step(/^the (.+) node is no longer outlined$/);
Step(/^I hover the (.+) node$/);
Step(/^I stop hovering the (.+) node$/);
Step(/^I see information for the (.+) node goes away$/);
Step(/^I click the "Fit to Screen" button$/);
Step(/^all (\d+) graph nodes are visible in the graph viewport$/);
Step(/^I click the "Zoom In" button$/);
Step(/^the visible graph scale increases$/);
Step(/^I press and hold the "Zoom In" button$/);
Step(/^the visible graph scale continues to increase$/);
Step(/^I click the "Zoom Out" button$/);
Step(/^the visible graph scale decreases$/);
Step(/^I press and hold the "Zoom Out" button$/);
Step(/^the visible graph scale continues to decrease$/);
Step(/^I turn the VS Code setting "Preferences: Color Theme" to "(.+)"$/);
Step(/^I can see that the background of the graph is "(.+)"$/);
Step(/^I can see that the arrowheads of the edges are "(.+)"$/);
Step(/^I click the Graph Scope button$/);
Step(/^I open the Graph Scope$/);
Step(/^I see two buttons for switching views between node type and edge type toggles$/);
Step(/^I select node types$/);
Step(/^I select edge types$/);
Step(/^I show only the (.+) edge type$/);
Step(/^I show no edge types$/);
Step(/^I toggle the (.+) edge on$/);
Step(/^I toggle the (.+) edge off$/);
Step(/^I toggle the (.+) node on$/);
Step(/^I toggle the (.+) node off$/);
Step(/^I show only the (.+) node type$/);
Step(/^I show only the (.+) node types$/);
Step(/^the Edge Types button is disabled$/);
Step(/^the Edge Types button is no longer disabled$/);
Step(/^I see a list of node types with toggles$/);
Step(/^I see a list of edge types with toggles$/);
Step(/^the available edge types are (?:only )?(.+)$/);
Step(/^the available (?:C\+\+|Go) node types are only (.+)$/);
Step(/^the available C# node types are only (.+)$/);
Step(/^the (.+) node type is not available for the (?:C\+\+|C#) example$/);
Step(/^the visible graph includes the (.+) node (.+) from (.+)$/);
Step(/^(.+) owns the (.+) node (.+)$/);
Step(/^the Signal node (.+) from (.+) connects to (.+)$/);
Step(/^the available Godot node types are (.+)$/);
Step(/^the visible graph shows (.+) in (.+) calling (.+) in (.+)$/);
Step(/^the visible graph shows (.+) in (.+) referencing type (.+) in (.+)$/);
Step(/^the visible graph shows (.+) in (.+) inheriting from (.+) in (.+)$/);
Step(/^the visible graph shows (.+) in (.+) implementing (.+) in (.+)$/);
Step(/^the visible graph shows (.+) in (.+) overriding (.+) in (.+)$/);
Step(/^I toggle the Imports edge on$/);
Step(/^I toggle the Folder node on$/);
Step(/^I toggle the Nests edge on$/);
Step(/^the Nests edge is toggled on$/);
Step(/^I close the Graph Scope$/);
Step(/^I can see a new "(.+)" node in the graph$/);
Step(/^I click and drag on the background I can select multiple nodes at once$/);
Step(/^I see all the selected nodes outlined in white$/);
Step(/^VS Code should navigate to the Explorer sidebar tab$/);
Step(/^the (.+) file should be highlighted in the Explorer$/);
Step(/^"(.+)" should be saved to my clipboard$/);
Step(/^the absolute path for (.+) should be saved to my clipboard$/);
Step(/^"(.+)" should be added to the "favorites" array in \.codegraphy\/settings\.json$/);
Step(/^the (.+) node should be centered in the middle of the graph$/);
Step(/^the filter section of the graph should expand$/);
Step(/^the add glob text should be prefilled with "(.+)"$/);
Step(/^a popup should appear titled "(.+)"$/);
Step(/^the legend group text should be prefilled with "(.+)"$/);
Step(/^a VS Code rename input should appear saying "(.+)"$/);
Step(/^the VS Code rename input should be prefilled with "(.+)"$/);
Step(/^a confirmation pops up saying "(.+)"$/);
