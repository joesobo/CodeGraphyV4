---
'@codegraphy-dev/core': minor
---

Make the CLI self-describing for agents, return data and errors in a stable JSON envelope, and store symbols and relations as queryable SQLite rows instead of embedding them only in file-analysis JSON.
