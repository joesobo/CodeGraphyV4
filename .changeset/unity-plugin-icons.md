---
"@codegraphy-dev/extension": patch
"@codegraphy-dev/plugin-unity": patch
---

Package built-in Unity plugin icon assets so theme legend rows and graph nodes can load their configured icons, use the real Material Icon Theme Unity icon for Unity defaults, and show an intentional fallback when an icon preview cannot load.
