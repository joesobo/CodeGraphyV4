---
"@codegraphy-dev/extension": minor
---

Replace the react-force-graph renderer with CodeGraphy's owned WebGPU renderer and deterministic main-thread WebAssembly typed-array physics kernel, including scalable Barnes–Hut charge, bounded TypeScript-managed fixed-timestep simulation with natural cooling, connection- and file-size-scaled nodes with zoom-aware legibility, size-weighted collision and repulsion behavior, size-aware visual layering and picking, exhaustive collision separation in dense graph regions, subdued hairline edges with hover highlighting, one-frame neighbor response and smooth release during dragging, natural clustering, lazy indexed interactions, antialiased Bézier edges, curve-aligned triangle arrows and self-loops, zoom-culled cached labels and arrows, automatic recovery from transient GPU resets, and an optional live potential-FPS and frame-time counter under Performance settings.
