---
"@codegraphy/extension": patch
---

Fix external CodeGraphy plugins failing to load on first open, keep timeline playback using the active commit's full graph data, and invalidate stale timeline caches whenever the available plugin set changes.
