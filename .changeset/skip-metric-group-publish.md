---
"@codegraphy-dev/extension": patch
---

Skip redundant legend group publication, deep graph reuse checks, full graph payloads, visible graph recomputation, pre-refresh legend broadcasts, and incremental freshness scans when a saved file only changes non-visual graph node metrics.
