---
"@codegraphy/extension": patch
"@codegraphy/plugin-markdown": patch
---

Fix focused TypeScript plugin views not appearing in the graph toolbar after plugin registration.

Improve 3D graph startup reliability so toggling into 3D no longer trips a startup race in the live VS Code extension.

Clarify the Markdown wikilink summary export so its label and filename explicitly identify it as Markdown-specific.
