---
"@codegraphy-dev/extension": patch
"@codegraphy-dev/plugin-unity": patch
---

Package built-in Unity plugin icon assets so theme legend rows and graph nodes can load their configured icons, use a white Material Icon Theme Unity glyph for Unity defaults, render `.asset` defaults with a triangle shape, and show an intentional fallback when an icon preview cannot load.
