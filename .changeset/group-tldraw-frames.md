---
"@codegraphy-dev/tldraw": minor
---

Group generated nodes with native tldraw frames. Draw a frame around nodes or
drag nodes into an existing frame while their icons, labels, connections, and
force physics stay active. Frame boundaries contain node collisions, moving or
resizing a frame updates its gravity center, and canvas refresh preserves
surviving frame membership.
